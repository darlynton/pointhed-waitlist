{
  "name": "pointhed-waitlist-backend",
  "version": "1.0.0",
  "description": "Pointhed Waitlist Landing Page Backend API",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "build": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:prod": "prisma migrate deploy",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "nodemailer": "^7.0.12",
    "resend": "^6.7.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
